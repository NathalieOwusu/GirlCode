// one time onboarding form to setup user profile (bio, tech stack(skills), Location, Github/portfolio link, profile picture)
// Populates what others see on discover cards
import React from 'react';

export default function Profile(): React.JSX.Element{
    return (
        <div>
            <h2> User Profile </h2>
        </div>
    )
    }