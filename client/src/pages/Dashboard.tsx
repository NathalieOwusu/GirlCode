// Central user hub (displays: Profile overview, recent matches, stats (Likes, matches, messages))
//Link to Editprofile, Matches and ChatRoom
import React from 'react';

export default function Dashboard(): React.JSX.Element{
    return (
        <div>
            <h2> Dashboard Page</h2>
        </div>
    )
    }